stranger — dependency proof
generated 2026-08-30T14:47:17Z by scripts/proof.sh

rustc: rustc 1.98.0 (88d9e12ae 2026-08-18)
cargo: cargo 1.98.0 (797e8a9bc 2026-08-05)

$ cargo tree
stranger v0.1.0 (/home/keir/stranger)

lines of cargo tree output: 1
  (one line means one crate: this one)

$ grep -c "^\[\[package\]\]" Cargo.lock
1
  (one [[package]] block means nothing was resolved but us)

$ grep -A2 "^\[dependencies\]" Cargo.toml
[dependencies]

[dev-dependencies]

--- the strongest one: it builds with the network unavailable ---
$ CARGO_NET_OFFLINE=true cargo build --release --locked --offline
OK

$ grep -q "forbid(unsafe_code)" src/lib.rs src/main.rs
src/lib.rs: forbidden
src/main.rs: forbidden

$ grep -rnE "\bunsafe\s*(\{|fn|impl|trait|extern)" src/
(no output — zero unsafe blocks, functions, impls or externs)

release binary: 4185728 bytes on Linux x86_64
  most of that is the 160066-name corpus, compiled in with include_str!
  so the tool needs no network and no cache directory at runtime.
