track = "A"
name = "stranger"
pitch = "Audits a dependency tree for hallucinated names, install-time code execution and version drift — reading lockfiles off disk, with zero dependencies and no network."
language = "rust"
repo = "https://github.com/keirsalterego/stranger"

# Bonus claims, all three checkable rather than claimable:
#   Reproducible Build — `make repro`, two directories, README "Reproducible build"
#   Package Killer     — serde_json, README "Package Killer" and STDLIB.md
#   STDLIB Log         — STDLIB.md, 18 entries
